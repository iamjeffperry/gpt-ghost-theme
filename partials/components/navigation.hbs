<header id="gh-navigation" class="gh-navigation is-left-logo{{#match @custom.header_and_footer_color "Accent color"}} has-accent-color{{/match}} gh-outer">
    <div class="gh-navigation-inner gh-inner">

        <div class="gh-navigation-brand">
            <div class="jp-navigation-identity">
                <a class="gh-navigation-logo is-title" href="{{@site.url}}">{{@site.title}}</a>
                {{#is "home"}}
                    <span class="jp-navigation-description is-body">
                        {{#if @site.description}}{{@site.description}}{{else}}Bits and pieces of technology, media, internet culture, games, and everyday life.{{/if}}
                    </span>
                {{/is}}
            </div>
            {{> "search-toggle"}}
            <button class="gh-burger gh-icon-button" aria-label="{{t "Menu"}}">
                {{> "icons/burger"}}
                {{> "icons/close"}}
            </button>
        </div>

        <nav class="gh-navigation-menu" aria-label="Primary navigation">
            <ul class="nav">
                <li class="nav-essays"><a href="{{@site.url}}/essays/">Essays</a></li>
                <li class="nav-notes"><a href="{{@site.url}}/notes/">Notes</a></li>
                <li class="nav-links"><a href="{{@site.url}}/links/">Links</a></li>
                <li class="nav-media"><a href="{{@site.url}}/media/">Media</a></li>
                <li class="nav-about"><a href="{{@site.url}}/about/">About</a></li>
            </ul>
        </nav>

        <div class="gh-navigation-actions">
            {{> "search-toggle"}}
            {{#if @site.members_enabled}}
                <div class="gh-navigation-members">
                    {{#if @member}}
                        <a class="gh-button" href="#/portal/account" data-portal="account">{{t "Account"}}</a>
                    {{else}}
                        {{#unless @site.members_invite_only}}
                            {{#unless hideSubscribeButton}}
                                <a class="gh-button" href="#/portal/signup" data-portal="signup">{{t "Subscribe"}}</a>
                            {{/unless}}
                        {{/unless}}
                    {{/if}}
                </div>
            {{/if}}
        </div>

    </div>
</header>
