{{!< default}}

<main class="gh-main gh-outer jp-type-archive">
    <section class="gh-archive gh-inner jp-type-archive-header">
        <div class="gh-archive-inner">
            <header class="gh-archive-wrapper">
                <h1 class="gh-article-title is-title">{{#if @custom.photo_label}}{{@custom.photo_label}}{{else}}Photos{{/if}}</h1>
                <p class="gh-article-excerpt is-body">Photographs, illustrations, and visual stories.</p>
            </header>
        </div>
    </section>

    <section class="gh-container jp-stream-container gh-outer">
        <div class="gh-container-inner gh-inner">
            <main class="gh-main jp-stream-main">
                <div class="gh-feed jp-stream">
                    {{#get "posts" filter="tag:[hash-photo,hash-image]" include="authors,tags" limit="100"}}
                        {{#foreach posts}}
                            {{> "post-card" lazyLoad=true}}
                        {{/foreach}}
                    {{/get}}
                </div>
            </main>
        </div>
    </section>
</main>
