{{!< default}}

<main class="gh-main gh-outer mf-type-archive">
    <section class="gh-archive gh-inner mf-type-archive-header">
        <div class="gh-archive-inner">
            <header class="gh-archive-wrapper">
                <h1 class="gh-article-title is-title">{{#if @custom.media_label}}{{@custom.media_label}}{{else}}Media{{/if}}</h1>
                <p class="gh-article-excerpt is-body">Podcasts, videos, and other things worth watching or listening to.</p>
            </header>
        </div>
    </section>

    <section class="gh-container mf-stream-container gh-outer">
        <div class="gh-container-inner gh-inner">
            <main class="gh-main mf-stream-main">
                <div class="gh-feed mf-stream">
                    {{#get "posts" filter="tag:hash-media" include="authors,tags" limit="100"}}
                        {{#foreach posts}}
                            {{> "post-card" lazyLoad=true}}
                        {{/foreach}}
                    {{/get}}
                </div>
            </main>
        </div>
    </section>
</main>
